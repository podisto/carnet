<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}carnet d'adresse{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('assets/vendor/bootstrap/dist/css/bootstrap.min.css') }}">
            <link rel="stylesheet" href="{{ asset('css/app.css') }}">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="jumbotron">
            <div class="container">
                <h1>Repository</h1>
                <p>Votre carnet d'adresse en ligne ou que vous soyez !!!</p>
                <p>
                    <a class="btn btn-primary btn-lg" href="{{ path('carnet_new') }}" role="button">Add contact</a>
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a class="btn btn-danger btn-lg" href="{{ path('fos_user_security_logout') }}" role="button">Logout</a>
                    {% endif %}
                </p>

            </div>
        </div>
        {% block body %}{% endblock %}
        {% block javascripts %}
            <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js"></script>
            <!-- Include all compiled plugins (below), or include individual files as needed -->
            <script src="{{ asset('assets/vendor/bootstrap/dist/js/bootstrap.min.js') }}"></script>
        {% endblock %}
    </body>
</html>
